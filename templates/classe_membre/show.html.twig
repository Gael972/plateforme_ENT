{% extends 'base.html.twig' %}

{% block title %}ClasseMembre{% endblock %}

{% block body %}
    <h1>ClasseMembre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe_membre.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classe_membre_index') }}">back to list</a>

    <a href="{{ path('classe_membre_edit', {'id': classe_membre.id}) }}">edit</a>

    {{ include('classe_membre/_delete_form.html.twig') }}
{% endblock %}
