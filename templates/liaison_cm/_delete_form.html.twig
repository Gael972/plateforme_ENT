<form method="post" action="{{ path('liaison_cm_delete', {'id': liaison_cm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liaison_cm.id) }}">
    <button class="btn">Delete</button>
</form>
