<form method="post" action="{{ path('note_matiere_delete', {'id': note_matiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note_matiere.id) }}">
    <button class="btn">Supprimer</button>
</form>
